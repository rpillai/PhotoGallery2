@using System.Web.Security.AntiXss
@model List<PhotoGallery2.Models.Comment>
<h4>Comments....</h4>
@foreach (var item in Model)
{
    <div class="row">
        <div class="col-sm-3">
            <p class="small label label-info"> <small>@item.User.FirstName</small></p>
        </div>
        <div class="col-sm-6">
            <p class="small text-left" style="-ms-word-wrap: break-word; word-wrap: break-word">@Html.Encode(item.Description)</p>
        </div>
    </div>
}

