@using System.Web.UI.WebControls
@using Microsoft.AspNet.Identity
@model PhotoGallery2.Models.ManageUserViewModel
<p>You're logged in as <strong>@User.Identity.GetUserName()</strong>.</p>

    @using (Html.BeginForm("Manage", "Account", FormMethod.Post, new { @class = "form-horizontal", style = "margin: 15px", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h4 class="caption">Change Password Form</h4>
        <br />
        <div class="form-group">
            <label class="col-md-4 control-label">Current Password</label>
            <div class="col-md-6">
                @Html.PasswordFor(m => m.OldPassword, new { @class = "form-control", autofocus = true, required = true })
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-4 control-label">New Password</label>
            <div class="col-md-6">
                @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control", required = true })
            </div>
        </div>

        <div class="form-group">
            <label class="col-md-4 control-label">Confirm New Password</label>
            <div class="col-md-6">
                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", required = true })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-4 col-md-6">
                <button type="submit" class="btn btn-primary">
                    <span class="glyphicon glyphicon-lock"></span>
                    Change password
                </button>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-4 col-md-10">
                @Html.ValidationSummary(false)
            </div>
        </div>
    }

